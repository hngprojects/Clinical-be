async def upload_file(file_content: bytes, filename: str, content_type: str) -> str:
    """
    Stub implementation — replace with real Cloudinary/Supabase upload.
    Returns a fake URL for testing purposes.
    """
    return f"https://storage.example.com/uploads/{filename}"